#user  nobody;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}

http {
    # log_format '$remote_addr - $remote_user [$time_local] '
    #            '"$request" $status $bytes_sent '
    #            '"$http_referer" "$http_user_agent" "$gzip_ratio"';
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile           off;
    tcp_nopush         on;
    keepalive_timeout  65;

    #gzip  on;

    server {
        # Preferred configuration. Changed to test with dashc.
        # listen              80;
        # server_name         panoplay.duckdns.org;
        # return              301 https://$server_name$request_uri;

        listen              80;
        server_name         panoplay.duckdns.org;
        add_header          Access-Control-Allow-Origin * always;
        add_header          Cache-Control no-cache always;
	
	location / {
            root  /var/www/master;
	    index index.html;
	}

	location = /generate_204 {
	    return 301 /;
	}

        location ~ \.php$ {
            include /usr/local/nginx/conf/fastcgi_params;
            fastcgi_intercept_errors on;
            fastcgi_pass unix:/var/run/php/php7.0-fpm.sock;
            fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;
        }
    }

    server {
        listen              443 ssl;
        server_name         panoplay.duckdns.org;
        ssl_certificate     /etc/ssl/certs/panoplay.duckdns.org.pem;
        ssl_certificate_key /etc/ssl/private/panoplay.duckdns.org.key;
        add_header          Access-Control-Allow-Origin * always;
        add_header          Cache-Control no-cache always;
	
	location / {
            root  /var/www/master;
	    index index.html;
	}

	location = /generate_204 {
	    return 301 /;
	}

        location ~ \.php$ {
            include /usr/local/nginx/conf/fastcgi_params;
            fastcgi_intercept_errors on;
            fastcgi_pass unix:/var/run/php/php7.0-fpm.sock;
            fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;
        }
    }
    
    # server {
    #     listen                4242 ssl;
    #     server_name           panoplay.duckdns.org;
    #     ssl_certificate       /etc/ssl/certs/panoplay.duckdns.org.pem;
    #     ssl_certificate_key   /etc/ssl/private/panoplay.duckdns.org.key;
    #     # ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;
    #     # ssl_certificate     /etc/ssl/certs/nginx-selfsigned.crt;
    #     add_header            Access-Control-Allow-Origin * always;
    #     add_header            Cache-Control no-cache always;
    #     root                  /var/www;

    #     location /panoplay/ {
    #         index     index.html index.htm index.php;
    #         autoindex on;

    #         location /panoplay/dev/ {
    #             auth_basic           "Restricted";
    #             auth_basic_user_file /usr/local/nginx/conf/.htpasswd;
    #         }
    #     }
    #     
    #     location /dev/ {
    #         index                index.html index.htm index.php;
    #         autoindex            on;
    #         auth_basic           "Restricted";
    #         auth_basic_user_file /usr/local/nginx/conf/.htpasswd;
    #     }
    # 
    #     location /horus/ {
    #         index                index.html index.htm index.php;
    #         autoindex            on;
    #         auth_basic           "Restricted";
    #         auth_basic_user_file /usr/local/nginx/conf/.htpasswd;
    #     }

    #     location /livedash/ {
    #         index                index.html index.htm index.php;
    #         autoindex            on;
    #         auth_basic           "Restricted";
    #         auth_basic_user_file /usr/local/nginx/conf/.htpasswd;
    #     }
    #     
    #     location /minibox/ {
    #         index                  index.html index.htm index.php;
    #         autoindex              on;
    #         auth_basic            "Restricted";
    #         auth_basic_user_file  /usr/local/nginx/conf/.htpasswd;
    #     }
    #     
    #     location ~ \.php$ {
    #         include /usr/local/nginx/conf/fastcgi_params;
    #         fastcgi_intercept_errors on;
    #         fastcgi_pass unix:/var/run/php/php7.0-fpm.sock;
    #         fastcgi_param SCRIPT_FILENAME $document_root/$fastcgi_script_name;
    #     }
    #     
    #     #charset koi8-r;

    #     #access_log  logs/host.access.log  main;
    #     
    #     #error_page  404              /404.html;

    #     # redirect server error pages to the static page /50x.html
    #     #
    #     error_page   500 502 503 504  /50x.html;
    #     location = /50x.html {
    #         root   html;
    #     }

    #     # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #     #
    #     #location ~ \.php$ {
    #     #    proxy_pass   http://127.0.0.1;
    #     #}

    #     # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #     #
    #     #location ~ \.php$ {
    #     #    root           html;
    #     #    fastcgi_pass   127.0.0.1:9000;
    #     #    fastcgi_index  index.php;
    #     #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
    #     #    include        fastcgi_params;
    #     #}
    # 
    #     # deny access to .htaccess files, if Apache's document root
    #     # concurs with nginx's one
    #     #
    #     #location ~ /\.ht {
    #     #    deny  all;
    #     #}
    # }
    
    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}
}
